<form method="post" action="{{ path('author_list', {'id': author_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author_list.id) }}">
    <button class="btn">Delete</button>
</form>