<form method="post" action="{{ path('category_list', {'id': category_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_list.id) }}">
    <button class="btn">Delete</button>
</form>